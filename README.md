# Python Excel Automation

This project contains a Python script to automate common Excel and CSV tasks.

## What it does
- Reads Excel or CSV files
- Cleans and organizes data
- Generates simple reports
- Converts between formats (Excel ↔ CSV)

## Use cases
- Data cleaning
- Repetitive Excel tasks
- Small business reports
- Custom automation for clients

## Technologies
- Python
- pandas
- openpyxl (if needed)

## Usage
The script can be easily adapted to client requirements.
Input and output formats are customizable.

## Notes
This project is intended as a base for custom automation jobs.

## How to run
1. Place your CSV file inside the `data` folder
2. Rename it to `input.csv`
3. Run:
   ```bash
   python excel_cleaner.py
